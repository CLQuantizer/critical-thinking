// import {test} from "vitest"
// import {db} from "$lib/server/db/index";
// import {sql} from "drizzle-orm";
// import {getWordStats} from "$lib/server/services/user";

// test.only("testDb", async () => {
//     // select 1 is a simple query that returns 1
//     await db.execute(sql`SELECT 1`);
// }, { timeout: 100000 });
//
// test.only("getWordStats", async () => {
//     const stats = await getWordStats(1);
//     console.log(stats);
// }, { timeout: 100000 });